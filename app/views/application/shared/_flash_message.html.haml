- flash.each do |type, message|
  .row
    .col-sm-offset-2.col-sm-10.col-xs-10.col-xs-offset-1
      %div{:class => "alert #{bootstrap_class_for(type)} fade in text-center", id: "flash"}
        %button.close{"data-dismiss" => "alert"} ×
        = message
    :javascript
      $(document).ready(function(){
        $("#flash").delay(1500).slideUp(500);
      });
