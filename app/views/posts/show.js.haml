\//kaminari 페이지네이션
$('#posts').html('#{escape_javascript render(partial: 'posts/common_components/tr')}');
$('#paginator').html('#{escape_javascript(paginate(@posts, remote: true).to_s)}');

commentEdit();
-# \//summernote 에디터 ajax대응
-# $('.redactor').each(function(){
-#   $(this).redactor();
-# });
